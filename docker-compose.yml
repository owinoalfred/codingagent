services:
  agent-api:
    build: ./agent-api
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./agent-api/workspace:/app/workspace
    command: uvicorn app:app --host 0.0.0.0 --port 8080
